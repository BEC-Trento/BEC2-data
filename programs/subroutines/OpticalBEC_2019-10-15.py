prg_comment = ""
prg_version = "0.7"
def program(prg, cmd):
    prg.add(0, "Initialize 0 TTL and Synchronize.sub")
    prg.add(150000, "Config field OFF")
    prg.add(160000, "[test] Degauss_1012")
    prg.add(12160000, "Set_MOT")
    prg.add(12260000, "Synchronize_MOT.sub")
    prg.add(12360000, "Switch Off MOT_fast")
    prg.add(12361250, "GM_051018")
    prg.add(12363150, "DAC MT-MOT Current", 0.0000, functions=dict(value=lambda x: cmd.get_var('MT_I')))
    prg.add(12363649, "DAC MT-MOT Voltage", 0.0000, functions=dict(value=lambda x: cmd.get_var('MT_Voltage')))
    prg.add(12416750, "wait")
    prg.add(12417750, "Config Field MT-MOT")
    prg.add(12418750, "DAC Horiz IR", 0.0000, functions=dict(value=lambda x: cmd.get_var('CigarV')))
    prg.add(12419750, "Oscilloscope 3 Trigger ON")
    prg.add(12424750, "Oscilloscope 3 Trigger OFF")
    prg.add(12425250, "AOM IR Horizontal Amp", 1000)
    prg.add(12425750, "AOM IR Horizontal freq", 80.00)
    prg.add(12426250, "DAC Vert IR", 2.0000, functions=dict(value=lambda x: cmd.get_var('XaxisTrap_V')))
    prg.add(12426750, "AOM IR Vertical Amp", 1000)
    prg.add(12427250, "AOM IR Vertical freq", 80.00)
    prg.add(12428500, "IGBT BCompY CLOSE")
    prg.add(12429250, "DAC BCompY", 0.4160, functions=dict(value=lambda x: cmd.get_var('BCompY_value')*30.0/18.))
    prg.add(12928500, "DAC MT-MOT Voltage", 8.5000)
    prg.add(14428500, "MT Current Ramp", start_t=0, stop_x=0, n_points=100, start_x=24, stop_t=500, functions=dict(start_x=lambda x: cmd.get_var('MT_I'), stop_x=lambda x: cmd.get_var('MT_I_final')))
    prg.add(14429250, "BCompY current ramp", start_t=0, stop_x=0.15, n_points=100, start_x=0.416, stop_t=500, functions=dict(start_x=lambda x: cmd.get_var('BCompY_value')*30.0/18., stop_x=lambda x: cmd.get_var('BCompY_value')*50.0/18.))
    prg.add(52928500, "MT Current Ramp", start_t=0, stop_x=18, n_points=500, start_x=0, stop_t=500, functions=dict(start_x=lambda x: cmd.get_var('MT_I_final')))
    prg.add(52929500, "BCompY current ramp", start_t=0, stop_x=0, n_points=500, start_x=0, stop_t=500, functions=dict(start_x=lambda x: cmd.get_var('BCompY_value')*50.0/18., stop_x=lambda x: cmd.get_var('BCompY_value')))
    prg.add(52929500, "MT Current HalfGauss ramp", start_t=0.0000, func_args="a=50, b=18, duration=3, width=2", n_points=500, func="(a - b * exp(-duration**2 / width**2)) / (1 - exp(-duration**2 / width**2)) + ((b - a) / (1 - exp(-duration**2 / width**2))) * exp(-(t - duration)**2 / width**2)", stop_t=3000.0000, functions=dict(func_args=lambda x: "a={}, b=18, duration=3000, width=1".format(cmd.get_var('MT_I_final')), funct_enable=False), enable=False)
    prg.add(57939500, "Horizontal Dipole Evaporation Ramp_Truncated")
    prg.add(77842600, "transfer_to_optical_trap")
    prg.add(88544700, "Synchronize.sub", enable=False)
    prg.add(88592200, "field_lock", enable=False)
    prg.add(88992200, "field_lock", functions=dict(time=lambda x: x + cmd.get_var('hold_time')), enable=False)
    prg.add(89042100, "DAC 100W_amplitude", 0.2000, functions=dict(time=lambda x: x + cmd.get_var('hold_time')), enable=False)
    prg.add(89172439, "Oscilloscope Trigger ON", functions=dict(time=lambda x: x + cmd.get_var('hold_time')), enable=False)
    prg.add(89182200, "Switch Off Dipole", functions=dict(time=lambda x: x + cmd.get_var('hold_time')), enable=False)
    prg.add(90292440, "DAC Horiz IR Exp ramp", start_t=0.0000, func_args="a=0.6, b=0.06, duration=1, tau=0.2", n_points=1500, func="(b - a * exp(-duration / tau) + (a - b) * exp(-t / tau)) / (1 - exp(-duration / tau))", stop_t=1000.0000, functions=dict(func_args=lambda x: 'a={0}, b={1}, duration=1, tau=0.2'.format(0.6, cmd.get_var('Cigar_end'))))
    prg.add(90294940, "Oscilloscope Trigger ON", enable=False)
    prg.add(100306000, "IR Horizontal HalfGauss ramp", start_t=0.0000, func_args="a=0, b=1, duration=1, width=0.5", n_points=200, func="(a - b * exp(-duration**2 / width**2)) / (1 - exp(-duration**2 / width**2)) + ((b - a) / (1 - exp(-duration**2 / width**2))) * exp(-(t - duration)**2 / width**2)", stop_t=1000.0000, functions=dict(func_args=lambda x: 'a={0}, b={1}, duration=1, width=0.5'.format(cmd.get_var('Cigar_end'), cmd.get_var('Cigar_compressed'))))
    prg.add(110307000, "wait")
    return prg
